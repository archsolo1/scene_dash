# Benchmark results

Save captured benchmark runs here (e.g. `2026-06-21-jit-desktop.txt`,
`release-aot-pixel.txt`), noting the date, device, build mode (JIT vs AOT) and
entity count. Keep raw output so regressions are comparable over time.

Nothing is committed here yet.
