name: scene_dash
description: >-
  A Bevy-inspired, Dart-native ECS and plugin runtime for flutter_scene.
  This is the pure-Dart core: entities, component stores, queries, systems,
  schedules, resources, events, commands, plugins and annotations.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.18.3

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
